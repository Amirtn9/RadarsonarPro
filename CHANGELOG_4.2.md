# SONAR Radar — نسخه ۴.۲

موضوع این نسخه یک چیز است: **قفل نشدن ربات وقتی یک کاربر کار سنگین می‌کند.**

---

## ۱. استخر ترد یکپارچه — `runtime.py` (فایل جدید)

**مشکل:** `core.run_remote_command` از `run_in_executor(None, ...)` استفاده می‌کرد.
`None` یعنی استخر پیش‌فرض asyncio با `min(32, cpu_count + 4)` ترد — روی VPS تک‌هسته‌ای فقط **۵ ترد**.
هر تست کانفیگ یک ترد را تا ۱۲۰ ثانیه نگه می‌داشت و `TunnelLogic.semaphore` هم ۵ تست همزمان اجازه می‌داد.
نتیجه: یک کاربر کل استخر را می‌بست و آمار سرور، نصب ایجنت و دانلود لوگوی گروه برای بقیه هنگ می‌کرد.

**اصلاح:**
- `runtime.SHARED_EXECUTOR` با ۴۸ ورکر (قابل تنظیم با `SONAR_EXECUTOR_WORKERS`)
- هر چهار استخر قبلی (`base.py` ۵۰ تایی، `cronjobs.py` ۱۰ تایی، `admin_panel.py` ۱۰ تایی، و پیش‌فرض) به همین یکی وصل شدند
- هیچ `run_in_executor(None, ...)` در پروژه باقی نمانده است
- هِلپر `run_sync(func, *args)` برای اجرای امن کد سینکرون

فایل‌های تغییریافته: `runtime.py`، `core.py`، `server_stats.py`، `topics.py`، `bot_logic_mod/base.py`، `bot_logic_mod/servers.py`، `admin_panel.py`، `cronjobs.py`

## ۲. دیتابیس از روی event loop برداشته شد

**مشکل:** `psycopg2` سینکرون است. `process_sub_async` به ازای **هر خط خروجی** یک `INSERT` جدا می‌زد، مستقیم روی event loop. برای یک اشتراک ۳۰۰ تایی یعنی ۳۰۰ بار قفل شدن کل ربات.

**اصلاح:** پارس کامل → یک `execute_values` دسته‌ای → داخل ترد.
کوئری‌های مسیر تست کانفیگ هم با `run_sync` بیرون از لوپ رفتند.

فایل‌های تغییریافته: `tunnel_logic.py`، `bot_logic_mod/tunnels.py`

## ۳. تست کانفیگ روی WebSocket، نه SSH

**مشکل:** تابع `_exec_via_ws` اسمش وب‌سوکت بود ولی `run_remote_command` (یعنی paramiko/SSH) صدا می‌زد.
هر کانفیگ = یک هندشیک SSH کامل + یک پروسه‌ی `python3 monitor_agent.py` جدید. با چند کاربر همزمان به سقف `MaxStartups` سرور ایران می‌خورد.

**اصلاح:**
- `ServerMonitor.ws_test_config(...)` جدید در `core.py` که از اکشن `test_config` ایجنت روی کانکشن پایدار استفاده می‌کند
- اکشن جدید `fetch_sub` در `monitor_agent.py`: فقط لیست کانفیگ‌های یک اشتراک را برمی‌گرداند و تست کردنشان موازی و سمت ربات انجام می‌شود
- فال‌بک `run_cmd` برای ایجنت‌های قدیمی که هنوز آپدیت نشده‌اند (سازگاری به عقب حفظ شده)
- مسیرهای «تست دقیق تکی» و «تست پینگ همگانی» هم به وب‌سوکت منتقل شدند

## ۴. صف عادلانه (Fair Queue)

**مشکل:** یک `Semaphore(5)` سراسری بین همه‌ی کاربرها مشترک بود. اگر کاربر A یک ساب ۲۰۰ تایی تست می‌کرد، کاربر B تا آخرش منتظر می‌ماند.

**اصلاح:** در `tunnel_logic.py`
- `GLOBAL_TEST_LIMIT = 20` (سقف کل)
- `PER_USER_TEST_LIMIT = 3` (سقف هر کاربر)

## ۵. باگ استخر وب‌سوکت — `ws_client.py`

کد قبلی:
```python
return self.ws and not getattr(self.ws, "closed", False)
```
در `websockets >= 14` آبجکت کانکشن صفت `closed` ندارد، پس `getattr` همیشه `False` برمی‌گرداند و **هر سوکت مرده‌ای «باز» تشخیص داده می‌شد**. دوباره وارد استخر می‌شد و درخواست بعدی تا تایم‌اوت روی آن می‌نشست.

**اصلاح:** تشخیص وضعیت از روی `ws.state` (با سازگاری به عقب برای API قدیمی)، به‌علاوه `_release` دیگر کانکشن بسته را به استخر برنمی‌گرداند.

## ۶. سقف بار روی خود ایجنت

حالا که تست‌ها موازی می‌روند، `monitor_agent.py` سقف خودش را دارد:
`AGENT_MAX_PARALLEL_TESTS` (پیش‌فرض ۶، قابل تنظیم با `SONAR_AGENT_MAX_TESTS`) + executor اختصاصی،
تا ده‌ها پروسه‌ی xray و curl همزمان سرور ایران را از پا درنیاورند.

## ۷. تنظیمات

| تنظیم | ۴.۱ | ۴.۲ | چرا |
|---|---|---|---|
| `WS_POOL_MAX_PER_KEY` | ۵ | ۲۰ | همه‌ی کاربرها روی یک نود = یک کلید استخر |
| `WS_ACQUIRE_TIMEOUT` | ۳۰ | ۴۵ | |
| `concurrent_updates` | `True` | ۲۵۶ | صریح |
| `connection_pool_size` | پیش‌فرض | ۲۵۶ | صف نشدن ارسال پیام‌ها |
| `websockets` | پین نشده | `>=13,<16` | همان باگ `closed` |

`PROJECT_VERSION` به `4.2` رسید. `post_shutdown` اضافه شد تا استخر وب‌سوکت و تردها تمیز بسته شوند.

---

## نکات نصب

```bash
pip install -r requirements.txt
systemctl restart sonar-bot
```

**مهم:** `monitor_agent.py` هم تغییر کرده. برای گرفتن کامل سود نسخه ۴.۲ باید روی نود مانیتورینگ آپدیت شود:
پنل ادمین ← مدیریت ایجنت ← نصب/آپدیت.
اگر آپدیت نکنید، ربات خودکار روی حالت فال‌بک `run_cmd` می‌رود و باز هم کار می‌کند، فقط کندتر.

### متغیرهای محیطی اختیاری

```bash
SONAR_EXECUTOR_WORKERS=48     # تعداد ترد ربات
SONAR_WS_POOL_MAX=20          # کانکشن همزمان به هر نود
SONAR_AGENT_MAX_TESTS=6       # تست همزمان روی ایجنت (سمت سرور ایران)
```

سرور ایران ضعیف است؟ `SONAR_AGENT_MAX_TESTS=3` بگذارید. قوی است؟ تا ۱۰ ببرید.
